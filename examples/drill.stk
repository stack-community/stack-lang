(掛け算ドリル) print
0 (score) var
1 11 1 range (i) (
    0 10 1 range rand (num1) var
    0 10 1 range rand (num2) var

    ( score 10 add (score) var (正解！おめでとうございます))
    ((残念。正解は) num1 num2 mul concat (でした) concat)
    i (問目: ) concat num1 concat ( x ) concat num2 concat ( = ) concat
    input num1 num2 mul equal if print
) for 
(結果: ) score concat (.pts) concat print