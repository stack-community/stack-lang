(
    (via) var
    (to) var
    (from) var
    (n) var

    (
        n 1 sub from via to hanoi eval
        from ( -> ) concat to concat print
        n 1 sub via to from hanoi eval
    )
    n 2 less not if

    (
        from ( -> ) concat to concat print
    )
    n 2 less if
)
(hanoi) var

(> ) input (a) (b) (c) hanoi eval